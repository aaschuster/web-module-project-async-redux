import {} from "../actions"

const initState = {
    isFetching: false,
    err: ""
}

export const reducer = (state = initState, action) => {
    switch (action.type) {
        default:
            return state;
    }
}

